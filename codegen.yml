schema:
  - schema/typedefs.ts
generates:
  types.ts:
    config:
      contextType: schema/context#BaseContext
      skipTypename: true

    plugins:
      - typescript
      - typescript-resolvers

require:
  - "ts-node/register/transpile-only"
